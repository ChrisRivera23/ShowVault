# Automatic venue discovery

ShowVault must be installable at an unknown venue without requiring the operator to pre-enter application paths, computer specifications, device addresses, or vendor inventory.

The primary outcome is to start ShowVault and find supported venue equipment and software represented in the integration catalog, including systems used in nightclubs, concert halls, houses of worship, and comparable production spaces.

`docs/INTEGRATION_CATALOG.md` is the authoritative first prototype testing matrix. Implemented integrations omitted from that matrix remain product capabilities but do not become prototype testing commitments.

## Intended workflow

1. Install the native client and Venue Agent.
2. Enroll the Agent and grant clearly explained operating-system permissions.
3. ShowVault inventories the host and identifies candidate applications, standard data locations, mounted storage, and local network interfaces.
4. The operator approves a bounded local recovery scope and, separately, the venue subnet to scan.
5. Product-specific plugins validate candidates before ShowVault credits them as protected systems.
6. The operator reviews findings and chooses what to protect.

Manual path and endpoint entry remains an advanced fallback, not the primary onboarding experience.

## Compatibility and direct-connection requirements

Discovery must work on a documented, tested range of older macOS and Windows venue computers rather than assuming current hardware or operating systems. The client, Agent, installer, local database, credential storage, and plugin runtime must each have explicit minimum versions. Unsupported systems must receive a clear explanation and practical fallback; release packaging must not silently raise minimum versions.

A laptop connected directly to one device by Cat5/Cat6 is a required test topology for non-networked equipment such as a grandMA2 console or Yamaha DM7. Directly connected private or IPv4 link-local interfaces follow the proposal, approval, bounded reachability, and separately authorized product-identification flow. Link-local proposals require exactly one qualifying active physical Ethernet interface; Wi-Fi and ambiguous multiple-link cases are excluded. Direct connection never permits configuration, synchronization, or generic-banner identification.

## Security boundary

Automatic discovery does not authorize backup or restore by itself. Candidate discovery is read-only and limited to documented standard locations. Each candidate records why it was found and remains marked as requiring operator approval. Approved paths are then converted into the Agent's exact local allowlist.

Network discovery must derive candidate interfaces and subnets locally, exclude loopback, tunnel, and obviously non-venue interfaces, and require a single explicit operator approval before bounded non-invasive discovery. IPv4 link-local is excluded except for the exact single-physical-Ethernet direct-link rule. ShowVault must not sweep arbitrary routed networks or treat generic reachability as product identification.

## Current slice

System inventory now includes bounded local recovery candidates and read-only local subnet proposals. A reusable catalog registry defines product-specific plugin identity plus standard macOS/Windows application and per-user data locations. Resolume Arena/Avenue have been migrated from dedicated host checks into that registry without changing their candidates. Serato DJ Pro detects `/Applications/Serato DJ Pro.app` on macOS, `Serato/Serato DJ Pro/Serato DJ Pro.exe` beneath Windows Program Files roots, and each local user's `Music/_Serato_` directory on both platforms. These locations follow Serato's official [application-start guidance](https://support.serato.com/hc/en-us/articles/224858107-Starting-the-Software), [Windows executable guidance](https://support.serato.com/hc/en-us/articles/360004267516-Troubleshooting-sign-in-activation), and [`_Serato_` library documentation](https://support.serato.com/hc/en-us/articles/204022904-What-is-in-the-Serato-folder). Missing and inaccessible locations are ignored and no real installed application has been inspected during development.

rekordbox uses its separate `showvault.rekordbox` identity. macOS detection covers the officially documented `rekordbox 5`, `rekordbox 6`, and `rekordbox 7` folders containing `rekordbox.app` beneath Applications. Windows detection enumerates at most 32 version directories matching the officially documented `Pioneer/rekordbox 5.*` family and requires the `rekordbox.exe` child. Per-user database candidates are `Library/Pioneer/rekordbox` on macOS and `AppData/Roaming/Pioneer/rekordbox` on Windows, following the official [rekordbox 5 FAQ](https://rekordbox.com/en/support/faq/rekordbox5/), [rekordbox 6 FAQ](https://rekordbox.com/en/support/faq/v6/), [rekordbox 7 FAQ](https://rekordbox.com/en/support/faq/v7/), and [database-removal guidance](https://support.pioneerdj.com/hc/en-us/articles/8443065790489-What-do-I-need-to-do-to-delete-all-databases). Current Windows 6/7 documentation exposes a desktop shortcut but not a stable executable path, so ShowVault does not guess one or treat the shortcut as product evidence.

Traktor Pro uses `showvault.traktor-pro`. Native Instruments documents Pro 2/3 application folders beneath `Applications/Native Instruments` and `Program Files/Native Instruments`, versioned root databases beneath each user's `Documents/Native Instruments/Traktor <version>`, and generated content under `Music/Traktor` on both platforms. Versioned roots use the same prefix-scoped, sorted 32-directory cap. These boundaries follow Native Instruments' official [installed files and folders guidance](https://support.native-instruments.com/support/solutions/articles/69000879348-native-access-what-files-and-folders-does-traktor-pro-3-install-on-my-system-).

VirtualDJ uses `showvault.virtualdj`. Installed-application detection checks `VirtualDJ.app` beneath macOS Applications and `VirtualDJ/virtualdj.exe` beneath Windows Program Files roots. Per-user recovery-data detection covers the current `Library/Application Support/VirtualDJ` macOS home and `AppData/Local/VirtualDJ` Windows home plus the legacy `Documents/VirtualDJ` home on both platforms. VirtualDJ documents these current and legacy homes in its official [transfer guidance](https://virtualdj.com/wiki/How%20to%20Transfer%20To%20A%20New%20Computer.html), which identifies the complete home as containing the database, playlists, history, sampler banks, plugins, skins, mappings, and other recovery material. Application locations follow the official [macOS installation guidance](https://virtualdj.com/manuals/virtualdj/quickstart/install/macinstall.html), [macOS clean-reinstall guidance](https://virtualdj.com/wiki/Mac%20Clean%20Re-install%20VirtualDJ%208.html), and [Windows clean-reinstall guidance](https://virtualdj.com/wiki/PC%20Clean%20Re-install%20VirtualDJ%208.html). Custom home folders and external-drive databases are not enumerated in this slice.

Engine DJ Desktop uses `showvault.engine-dj`. Installed-application detection checks `Engine DJ.app` beneath macOS Applications and `Engine DJ/Engine DJ.exe` beneath Windows Program Files roots, following the official [desktop downloads](https://enginedj.com/downloads) and Engine DJ-hosted [macOS process-path](https://community.enginedj.com/t/engine-dj-4-0-memory-leak-on-mac/58437/15) and [Windows executable-path](https://community.enginedj.com/t/stems-and-playlist-creation-with-tracks-on-linux-with-wine/61309) evidence. Per-user recovery-data detection covers `Music/Engine Library` on both platforms. Engine DJ's official [library-corruption and recovery guidance](https://support.enginedj.com/support/solutions/articles/69000815206) identifies that folder as the computer's metadata database and documents its local backup, while explicitly excluding external-drive databases from that backup.

Denon Engine OS removable media uses a separate `showvault.engine-os` identity and `RemovableDataRoot` candidate type. The Agent considers at most 64 ready mounted roots: `/Volumes/*` on macOS and non-system fixed or removable drives on Windows. It checks only for an `Engine Library` directory at each root. Engine DJ's official [external-drive corruption guidance](https://support.enginedj.com/support/solutions/articles/69000815206) and [Engine DJ 2 migration guidance](https://support.enginedj.com/support/solutions/articles/69000797867) both document `Engine Library/Database2` at the external-drive root, while the official [drive-setup guidance](https://support.enginedj.com/support/solutions/articles/69000826536-engine-dj-setting-up-your-drive) establishes SD cards and USB mass-storage drives as Engine OS media. Discovery does not open `Database2`, inspect tracks, identify attached hardware, or imply that the drive is validated, protected, or usable.

Algoriddim djay Pro uses `showvault.djay-pro`. Installed-application detection checks `djay.app` beneath macOS Applications and the current `59BEBC1A.djay_e3tqh12mt5rj6` Microsoft Store package beneath each Windows user's `AppData/Local/Packages`. Per-user recovery-data detection covers `Music/djay` on both platforms, the macOS group-container `Library/Application Support/Algoriddim` analysis root, and the Windows package's `LocalCache/Local/Algoriddim/djay` analysis/settings root. These locations follow Algoriddim's official [stored-data guidance](https://help.algoriddim.com/topic/using-djay/where-does-djay-store-data), [macOS manual](https://help.algoriddim.com/user-manual/djay-pro-mac/tips/stored-data), and [Windows manual](https://help.algoriddim.com/user-manual/djay-pro-windows/tips/stored-data). Legacy djay Pro 1/2 locations and removable OneLibrary media are not enumerated in this slice.

Mixxx uses `showvault.mixxx`. Installed-application detection checks `Mixxx.app` beneath macOS Applications and `Mixxx/Mixxx.exe` beneath Windows Program Files roots. Per-user recovery-data detection covers the current `%LOCALAPPDATA%/Mixxx` Windows settings directory, the sandboxed `Library/Containers/org.mixxx.mixxx/Data/Library/Application Support/Mixxx` macOS 2.3-and-later settings directory, and the documented `Library/Application Support/Mixxx` macOS 2.2-and-earlier directory. Mixxx documents that the complete settings directory contains its library database, preferences, controller mappings, broadcast profiles, effects, samplers, sound configuration, and other user data and recommends copying the entire directory for backup in its official [settings-directory guidance](https://manual.mixxx.org/2.5/en/chapters/appendix/settings_directory.html). Application locations follow the official [installation guidance](https://manual.mixxx.org/2.5/en/chapters/getting_started.html) and Mixxx's official [Windows troubleshooting guidance](https://github.com/mixxxdj/mixxx/wiki/troubleshooting). The obsolete Windows XP migration directory, user-selected music libraries, and external drives are not enumerated in this slice.

disguise Designer uses `showvault.disguise-designer`. Windows detection covers only the default per-user `Documents/d3 Projects` root documented by disguise's [project-location guidance](https://help.disguise.one/designer/content-management/project-location) and [sample-project installation guidance](https://help.disguise.one/cloud/disguise-drive/designer-sample-projects). disguise's official [installation guidance](https://help.disguise.one/designer/getting-started/download-install) permits both the program directory and project directory to be changed, so ShowVault does not guess an executable path or enumerate non-default project roots. Detection checks only whether the default root exists and does not read `.d3`/`.d3Starter` files or other project content.

Dataton WATCHOUT 7 uses `showvault.watchout`. Installed-application detection checks the system-drive `WATCHOUT7` directory because Dataton's official [installation guidance](https://docs.dataton.com/guide/watchout/getting-started/installing-watchout.html) defines `C:\WATCHOUT7` as the default while allowing a custom `/D=` directory. Dataton's [save guidance](https://docs.dataton.com/guide/watchout/working-with-shows/saving-your-work.html) says Producer prompts for a location and filename, and the installation guide identifies `<install-dir>\.wo` as the default working/assets directory rather than a show file. ShowVault therefore does not guess custom installs or project roots. Detection checks only whether the default installation directory exists and does not read application, cache, asset, or show content.

Green Hippo Hippotizer V4 uses `showvault.hippotizer`. Windows installed-application detection checks only `GreenHippo/HippotizerV4` beneath the native Program Files root, following Green Hippo's official [Hippotizer 4.8.5 installation notes](https://support.green-hippo.com/hc/en-gb/articles/27928518937490-Hippotizer-version-4-8-5), which publish `C:\Program Files\GreenHippo\HippotizerV4`. The official [Show Manager guidance](https://www.manula.com/manuals/green-hippo/hippotizer-v4/4.8.5/en/topic/show-manager) exports show components to a user-defined directory, [New Show guidance](https://www.manula.com/manuals/green-hippo/hippotizer-v4/4.8.5/en/topic/hl-import-export-show) imports a user-selected directory, and [media-backup guidance](https://www.manula.com/manuals/green-hippo/hippotizer-v4/4.8.5/en/topic/backing-up-the-media-library) makes the STRATA path configurable and locally displayed. ShowVault therefore does not guess a project, show-export, media, or 32-bit Program Files location. Detection checks existence only and reads no Hippotizer content.

AV Stumpfl PIXERA uses `showvault.pixera`. Windows installed-application detection enumerates at most 32 `build_` version directories beneath native `Program Files/AV Stumpfl/Pixera` and requires each documented `presence` child directory. AV Stumpfl's official [Director–Client setup](https://help.pixera.one/manager-client-setup) publishes `C:\Program Files\AV Stumpfl\Pixera\build_x-x-x\presence`. Its [project save/load guidance](https://help.pixera.one/project-management/saving-and-loading-projects) describes saving to disk and selecting a project through an open dialog without publishing a stable project root, so ShowVault does not guess one or enumerate 32-bit/custom application paths. Detection checks existence only and reads no application or project content.

Christie Pandoras Box uses `showvault.christie-pandoras-box`. Windows installed-application detection enumerates at most 32 `Pandoras Box <version>` directories beneath native `Program Files/Christie` and requires the documented `PandorasBox.exe` child. Christie's official [Pandoras Box download center](https://www.christiedigital.com/products/media-servers-and-players/pandoras-box-download-center/) publishes the version 8.3 user manual, whose installation and SDK guidance documents the default `C:\Program Files\Christie` root and a typical `Pandoras Box 8.1.0` version directory containing `PandorasBox.exe`. The same [official user manual](https://www.christiedigital.com/globalassets/resources/public/pandorasbox/pandoras-box-help-pb-8.3.0-and-wd-6.5.1.pdf) states that the default installation root can be changed and that first save, export, copy, and bundle actions use operator-selected project locations. ShowVault therefore does not guess custom or 32-bit installs, project/content roots, or media locations. Detection checks executable existence only and reads no application or project content. The later catalog row named Pandora's Box Manager is not a distinct current installation target: Christie's official [V8 licensing announcement](https://www.christiedigital.com/press-releases/pandoras-box-v8-software-license/) says one V8 software license replaced the former separate Server, Player, Compact Player, Software Player, Manager, and Educational licenses and unlocks the combined feature suite. The existing candidate therefore covers current V8 software installation only; it does not inspect or assert activation, enabled Manager capability, legacy standalone Manager licensing, project data, validation, protection, backup, verification, or restore support.

Derivative TouchDesigner uses `showvault.touchdesigner`. macOS detection checks only the standard `/Applications/TouchDesigner.app` bundle. Windows detection enumerates at most 32 `TouchDesigner.<build>` directories beneath native `Program Files/Derivative` and requires `bin/TouchDesigner.exe`. Derivative's official [installation guidance](https://docs.derivative.ca/Install_TouchDesigner) documents dragging the app into Applications on macOS and the current versioned Windows layout, while also allowing renamed or arbitrarily located macOS builds and custom Windows install directories. Official [Project Packager guidance](https://docs.derivative.ca/Project_Packager) requires an operator-provided project folder and `.toe` file. ShowVault therefore does not guess renamed/custom or 32-bit installs, project roots, `.toe` locations, or media. Detection checks existence only and reads no application or project content.

HeavyM automatic local detection is deliberately deferred. Official [installation guidance](https://help.heavym.net/hc/en-us/articles/360018638240-Installing-HeavyM-Registering-a-License) tells operators to launch the downloaded `.exe` or `.dmg` and follow the installer without publishing a stable Windows executable or macOS application-bundle location. Official [menu guidance](https://help.heavym.net/hc/en-us/articles/360018823419-Menus) states that Save As asks the operator to browse to and select where each `.hm` project folder is created. Official [Collect & Save guidance](https://help.heavym.net/hc/en-us/articles/6611135681810-Collect-Save-and-file-handling) defines the portable project folder and documents `Documents/HeavyM/Project Backups` as a secondary safety copy, not the authoritative project root. ShowVault therefore adds no catalog entry, guesses no path, reads no candidate content, and credits no HeavyM installation, project, validation, protection, verification, restore, or media support. Reconsider only if primary documentation publishes dependable standard locations or a representative fixture establishes a safe bounded detection contract.

MadMapper 6 uses `showvault.madmapper`. Installed-application detection enumerates at most 32 versioned `MadMapper 6.` directories and requires the official executable beneath each: `Contents/MacOS/MadMapper` inside `/Applications/MadMapper 6.x.app` on macOS, or `MadMapper.exe` inside native Windows `C:\Program Files\MadMapper 6.x`. Exact project candidates are `.madproject` workspace directories beneath each user's documented default `Documents/MadMapper` root, with the same 32-directory bound. MadMapper's official [software-installation guidance](https://docs.madmapper.com/madmapper/6/13.-setup-and-installation/software-installation) directs macOS users to Applications, and its [mass-deployment examples](https://docs.madmapper.com/madmapper/6/13.-setup-and-installation/software-installation/mass-deploy-installation) publish both versioned executable layouts. Official [project-file guidance](https://docs.madmapper.com/madmapper/6/2.-the-interface/madmapper-project-file-structure) defines `.madproject` as the self-contained workspace and publishes `Documents/MadMapper/<name>.madproject` as its default location. Detection checks existence and directory names only; it reads no executable, `.mad`, autosave, resource, or media content. Legacy `.mad` files, custom project locations, unversioned/custom applications, 32-bit Windows installs, validation, protection, verification, and restore remain unsupported.

TroikaTronix Isadora 4 uses `showvault.isadora`. Installed-application detection checks only the documented usual `/Applications/Isadora 4/Isadora.app` bundle on macOS and `C:\Program Files\Isadora 4` directory on Windows. TroikaTronix's official [multiple-version installation guidance](https://support.troikatronix.com/support/solutions/articles/13000014942-how-to-install-multiple-versions-of-isadora) publishes those version folders, while permitting renamed copies and describing `.izz` documents as files opened from their existing location. Official [project workflow guidance](https://troikatronix.com/add-ons/?u=troikatronixofficial) recommends creating a local project folder and saving a new Isadora file into it rather than publishing a standard project root. ShowVault therefore does not scan for `.izz` files or guess renamed/custom applications, 32-bit installs, project folders, or media. Detection checks application-directory existence only, reads no candidate contents, and credits no Isadora validation, protection, verification, backup, or restore support.

OBS Studio uses `showvault.obs-studio`. Installed-application detection checks only `/Applications/OBS.app` on macOS and `obs-studio/bin/64bit/obs64.exe` beneath the native Windows Program Files root. The official OBS Project [macOS installation guide](https://obsproject.com/kb/mac-installation) directs users to drag `OBS` to Applications, while its [Windows GPU selection guide](https://obsproject.com/kb/gpu-selection-guide) publishes the exact native executable path. Per-user recovery candidates are limited to `Library/Application Support/obs-studio/basic/profiles` and `Library/Application Support/obs-studio/basic/scenes` on macOS, or `AppData/Roaming/obs-studio/basic/profiles` and `AppData/Roaming/obs-studio/basic/scenes` on Windows. OBS's official source fixes the [`profiles`](https://github.com/obsproject/obs-studio/blob/220a16378fb9079b0ebd008ee994f5cb298de90d/frontend/widgets/OBSBasic_Profiles.cpp) and [`scenes`](https://github.com/obsproject/obs-studio/blob/220a16378fb9079b0ebd008ee994f5cb298de90d/frontend/widgets/OBSBasic_SceneCollections.cpp) subdirectories and resolves the config base through [Windows roaming AppData](https://github.com/obsproject/obs-studio/blob/220a16378fb9079b0ebd008ee994f5cb298de90d/libobs/util/platform-windows.c) and [macOS user Application Support](https://github.com/obsproject/obs-studio/blob/220a16378fb9079b0ebd008ee994f5cb298de90d/libobs/util/platform-cocoa.m). Official [profile guidance](https://obsproject.com/kb/profiles) distinguishes profile settings from scene collections and supports export for backup. Detection checks existence only and reads no profile, scene, credential, recording, media, plugin, or log content. Portable mode and custom/Steam application locations are safe false negatives, and no OBS validation, protection, backup, verification, or restore support is credited.

QLab automatic local detection is deliberately deferred. Figure 53's official [Preparing Your Mac guidance](https://qlab.app/docs/v5/general/preparing-your-mac/) requires QLab to reside in the Mac's Applications folder, establishing `/Applications/QLab.app` as a dependable application candidate. However, official [workspace-management guidance](https://qlab.app/docs/v5/fundamentals/managing-workspaces/) presents a standard macOS save panel and requires the operator to choose where each workspace or project folder is created. Official [workspace-settings guidance](https://qlab.app/docs/v5/fundamentals/workspace-settings/) stores automatic backups in a workspace-named folder beside that already user-selected workspace. No fixed application-support, Documents, or other bounded recovery root is published. ShowVault therefore adds no catalog entry or filesystem search, does not infer workspaces from `.qlab5` or backup filenames, and reads no QLab application, workspace, media, license, preference, or backup content. No QLab installation, workspace identification, validation, protection, backup, verification, or restore support is credited. Reconsider only if Figure 53 publishes a dependable standard workspace root or a representative fixture establishes a separately approved bounded contract.

SCS (Show Cue System) automatic local detection is deliberately deferred. Show Cue Systems' official [Starting SCS guidance](https://www.showcuesystems.com/scs11help/topics/scs-starting.htm) publishes `C:\Program Files\SCS 11\scs11.exe`, but its `Documents\SCS Files\Show_Monday.scs11` cue file is presented only as a command-line example. Official [Special Folders guidance](https://showcuesystems.com/scs11help/topics/special-folders.htm) says the first file browser defaults broadly to Documents, defines the Production Folder as optional, and reserves per-user `AppData\Roaming\ShowCueSystem` for device maps and other machine-specific application data rather than the portable production. Official [backup-computer guidance](https://showcuesystems.com/scs11help/topics/backup-computer.htm) directs operators to collect production files and then copy that folder to the desired location on another computer. No fixed bounded cue-file or portable-production root is published. ShowVault therefore adds no catalog entry or filesystem search, does not treat the broad Documents directory as an SCS show, and does not infer productions from `.scs11`, recent-file, favorite-file, or device-map data. No SCS installation, show identification, validation, protection, backup, verification, or restore support is credited. Reconsider only if Show Cue Systems publishes a dependable standard production root or a representative fixture establishes a separately approved bounded contract.

PlaybackPro automatic local detection is deliberately deferred. DT Videolabs' official [PlaybackPro Plus guide](https://www.dtvideolabs.com/user-guide-playbackproplus/) directs operators to place an unzipped application in the macOS Applications folder, but does not publish an exact bundle name/path that safely resolves the unversioned PlaybackPro catalog row across the legacy PlaybackPro, PlaybackPro Plus, and PlaybackPro Plus X products listed in its official [user-guide index](https://www.dtvideolabs.com/dt-videolabs-user-guides/). The guides describe saving playlists or show files through the application without publishing a standard save root. They also state that playlist clips merely reference original media files, expose each referenced file path, and require the operator to locate moved/offline clips. No fixed bounded playlist, show, or media root is established. ShowVault therefore adds no catalog entry or filesystem search, guesses no bundle or show-file extension, and reads no playlist, media, license, preference, or network-control content. No PlaybackPro installation, playlist/show identification, validation, protection, backup, verification, or restore support is credited. Reconsider only if DT Videolabs publishes an exact supported product identity plus dependable standard application and recovery roots, or a representative fixture establishes a separately approved bounded contract.

Mitti automatic local detection is deliberately deferred. Imimot's official [license-management guidance](https://imimot.com/help/mitti/license-management/) identifies the application bundle and executable as `Mitti.app/Contents/MacOS/Mitti`, but does not publish a dependable standard installation location. Official [Bundle Playlist guidance](https://imimot.com/help/mitti/misc/) says the command creates an operator-named directory containing the project file and a `files` media directory, without publishing a standard parent location. It also says that the bundled project continues using media at the original locations when those files remain available and only falls back to the adjacent `files` directory when they do not. The same guidance permits manually placing media beside an operator-located `.mitti2` project or in an adjacent `files` directory. No fixed bounded project or authoritative media root is established. ShowVault therefore adds no catalog entry or filesystem search, does not infer `/Applications/Mitti.app`, scan for `.mitti2` files, enumerate broad home or media directories, or read application, project, media, license, preference, log, or network-control content. No Mitti installation, project identification, validation, protection, backup, verification, or restore support is credited. Reconsider only if Imimot publishes dependable standard application and recovery locations or a representative fixture establishes a separately approved bounded contract.

ProPresenter uses `showvault.propresenter`. Installed-application detection checks only `/Applications/ProPresenter.app` on macOS and `Renewed Vision/ProPresenter` beneath the native Windows Program Files root. Renewed Vision's official [ProPresenter 7 guide](https://files.renewedvision.com/propresenter/support/Pro7UserGuide.pdf) directs operators to move the downloaded macOS application into Applications, and its current official [download archive](https://renewedvision.com/downloads/propresenter/mac/ProPresenter_21.4_352583705.zip) identifies that bundle as `ProPresenter.app`. Official [Windows graphics guidance](https://support.renewedvision.com/hc/en-us/articles/14275292768915-Setting-ProPresenter-to-High-Performance-Graphics-Mode) publishes the default `C:\Program Files\Renewed Vision\ProPresenter` application directory. Per-user recovery detection is limited to `Documents/ProPresenter` on both platforms: Renewed Vision identifies it as the default ProPresenter content location on [macOS](https://support.renewedvision.com/hc/en-us/articles/7054191440787-Removing-Files-from-iCloud-Drive) and [Windows](https://support.renewedvision.com/hc/en-us/articles/7052659684627-Removing-Files-from-OneDrive), while its [Windows uninstall guidance](https://support.renewedvision.com/hc/en-us/articles/360041345074-Uninstalling-ProPresenter-on-Windows) says the default root contains the library, support files, and media repository. Detection checks existence only and reads no application, presentation, playlist, library, media, registration, preference, log, or other candidate content. Custom support/workspace locations, cloud-backed Documents redirections, external media references, and search paths are safe false negatives. No ProPresenter validation, protection, backup, verification, or restore support is credited.

PVP (ProVideoPlayer) automatic local detection is deliberately deferred. Renewed Vision's official [installation guidance](https://support.renewedvision.com/hc/en-us/articles/360011792273-How-do-I-install-ProVideoPlayer) directs operators to unzip the macOS application and place it in Applications, but does not publish an exact bundle path needed for a fixed catalog candidate. More importantly, official [Show guidance](https://support.renewedvision.com/hc/en-us/articles/360011791233-What-is-a-Show) defines each Show as an encapsulated file while explicitly excluding its media, without publishing a standard save root. Official [media-management guidance](https://support.renewedvision.com/hc/en-us/articles/360011696174-Media-Management-in-PVP-3) says PVP does not copy media to a managed location and instead stores links to the original absolute paths. The optional [relative-media workflow](https://support.renewedvision.com/hc/en-us/articles/360011791753-How-do-I-use-the-Use-Relative-Media-Paths-feature) requires the operator to create a folder, save the Show within it, and choose where that folder lives. No fixed bounded Show or authoritative media root is established. ShowVault therefore adds no catalog entry or filesystem search, guesses no application bundle or Show-file extension, and reads no Show, media, registration, preference, or network-backup content. No PVP installation, Show identification, validation, protection, backup, verification, or restore support is credited. Reconsider only if Renewed Vision publishes exact standard application and recovery locations or a representative fixture establishes a separately approved bounded contract.

CuePilot automatic local detection is deliberately deferred. CuePilot ApS's official [installation guidance](https://cuepilot.zendesk.com/hc/en-us/articles/360052909032-Downloading-and-Installing-the-CuePilot-Application) directs macOS users to place CuePilot in Applications, but its Windows instructions identify only the downloaded `CuePilotSetup.exe` and do not publish a stable installed application path. The same guidance says project information is saved to the CuePilot Cloud Server and can be recovered by reinstalling and signing in. Official [project guidance](https://cuepilot.zendesk.com/hc/en-us/articles/360050897851-Creating-Projects) clarifies that the included SOLO project is stored locally, but publishes no local storage path. Official [export guidance](https://cuepilot.zendesk.com/hc/en-us/articles/360055886132-Export-and-Import-Acts-Rundowns-and-Projects) requires the operator to choose where exported `.cue` files are saved. Official [media guidance](https://cuepilot.zendesk.com/hc/en-us/articles/360055610131-Add-and-Share-Media) lets operators select arbitrary local files and optionally upload optimized copies to CuePilot's cloud. No fixed bounded authoritative project or media root is established. ShowVault therefore adds no catalog entry or filesystem search, does not infer a Windows installation, macOS bundle name, local cache, SOLO-project location, cloud state, export location, or media root, and reads no account, project, cue, media, preference, credential, or network content. No CuePilot installation, project identification, validation, protection, backup, verification, or restore support is credited. Reconsider only if CuePilot ApS publishes exact standard application and local recovery locations or a representative fixture establishes a separately approved bounded contract.

Ventana automatic local detection is deliberately deferred because the catalog label cannot be resolved to a unique professional playback product from primary sources. The official [Ventuz product site](https://www.ventuz.com/) identifies Ventuz—not Ventana—as the real-time graphics suite already tracked separately in this catalog. The official [VNTANA platform site](https://www.vntana.com/platform/3d-cms/) describes a cloud 3D content-management and publishing platform, while [Ventana Systems](https://vensim.com/software/) publishes Vensim system-dynamics simulation products. None establishes a local show application named Ventana, its vendor, an application identity, a stable installation path, or a project root. ShowVault therefore adds no catalog entry or scanner, guesses no association, reads no workstation content, and credits no Ventana installation, project, validation, protection, verification, backup, restore, hardware, or media support. Reconsider only when the Product Owner supplies a vendor/product identity or authoritative documentation establishes one.

Millumin automatic local detection is deliberately deferred. The official [Millumin website](https://www.millumin.com/) identifies the product as a macOS application and provides its download, while [getting-started guidance](https://help.millumin.com/docs/general/getting-started/) tells operators to download and run Millumin without publishing a stable application-bundle path. Official [project guidance](https://help.millumin.com/docs/general/tips/) documents portable `.millumin` files and a `collect files` workflow that creates an operator-selected folder around the project file, but publishes no standard project root. ShowVault therefore adds no catalog entry, guesses no path, reads no candidate content, and credits no Millumin installation, project, validation, protection, verification, or restore support. Reconsider only if primary documentation publishes dependable standard locations or a representative fixture establishes a safe bounded detection contract.

Ventuz automatic local detection is deliberately deferred. Official [installation guidance](https://www.ventuz.com/support/help/latest/HowTo/HowToGetVentuzRunning.html) requires Windows but directs the operator to choose the installation location. Official [project guidance](https://www.ventuz.com/support/help/latest/ProjectsAndScenes.html) defines a complete `.vzp`-anchored project folder and its portable assets, revisions, and archives, but likewise directs the operator to choose its location. ShowVault therefore adds no catalog entry, guesses no path, reads no candidate content, and credits no Ventuz installation, project, validation, protection, verification, or restore support. Reconsider only if primary documentation publishes dependable standard locations or a representative fixture establishes a safe bounded detection contract.

Detected paths remain only in Agent SQLite. The Agent publishes opaque IDs plus bounded product/type/evidence metadata; the control plane persists tenant-scoped decisions, and the native dashboard supports manager approve/reject review without receiving paths. Protocol 1.3 delivers path-free decisions to the originating Agent, which resolves only locally known IDs and idempotently adds or removes durable exact scopes. Protocol 1.4 validates an approved Resolume user-data candidate through the real plugin and stores its hashed discovery result locally. The control plane correlates path-free outcomes to the unique validation command, native onboarding displays pending/passed/failed state and file count, and only the latest passing result can start backup without a path. Unknown IDs fail without granting access, installed applications are not accepted as recovery roots, and a new decision clears stale validation evidence. OBS Studio, disguise Designer, WATCHOUT, Hippotizer, PIXERA, Christie Pandoras Box, TouchDesigner, MadMapper, Isadora, Serato, rekordbox, Traktor, VirtualDJ, Engine DJ, djay Pro, Mixxx, and Engine OS currently stop at detection and approval: no product discovery plugin is registered, candidate contents are not read, and validation, backup, verification, and restore are not credited.

Subnet derivation considers active physical Ethernet and Wi-Fi interfaces with private IPv4 unicast addresses and contiguous usable masks. It excludes loopback, tunnel, PPP, VPN/virtual/container/bridge-like interfaces, public addresses, /31-/32 prefixes, and network or broadcast addresses. Protocol 1.10 additionally permits one IPv4 link-local proposal only when exactly one active physical Ethernet interface qualifies; Wi-Fi and multiple qualifying Ethernet interfaces produce no link-local proposal. Private networks broader than /24 are narrowed to /24. Link-local presents the full `169.254.0.0/16` scope for explicit review because self-assigned peers need not share a /24, but later active discovery remains capped at 32 exact targets. Proposals are deduplicated and capped at eight, evidence does not expose the host address, and collection opens no sockets or contacts hosts. Approval does not authorize or start discovery.

Protocol 1.11 improves an approved link-local discovery without sweeping the `/16`. Immediately before the separately authorized ICMP run, the Agent executes the operating system's read-only `arp` command with a two-second timeout and a 256 KiB output cap, waits through a cancellable one-second passive observation window, revalidates that the same single physical Ethernet interface still qualifies, and reads the cache once more. It accepts only complete IPv4 entries from that exact interface, filters them to the approved `169.254.0.0/16`, excludes the Agent/network/broadcast addresses, merges and deduplicates at most 64 passive candidates, and prioritizes them before fallback. The final active target set still contains at most 32 addresses total, uses 100-500 ms timeouts and concurrency eight, and stores responders only in Agent SQLite. If the cache and responder set are empty, native onboarding advises keeping the direct Ethernet link connected and retrying so another passive announcement can be observed. The bounded sequential fallback remains; discovery cannot guarantee a peer that has produced no observable neighbor entry.

Protocol 1.12 adds path-free target diagnostics to that exact discovery outcome. `passiveCandidateCount` reports how many of the actively checked targets came from the filtered neighbor cache, and `fallbackTargetCount` reports how many came from bounded sequential fallback. Both are non-negative and must sum exactly to `attemptedHostCount`; no addresses, cache contents, interface names, or MAC addresses leave the Agent. The control plane persists the counts with the correlated discovery result, and native onboarding displays them beside attempted/responding totals while retaining the reachability-only warning.

The direct-link fixture harness now covers populated and empty macOS- and Windows-shaped ARP tables through Agent-local proposal approval, durable command execution, passive-prioritized bounded discovery, responder retention, and path-free completion evidence. Populated fixtures continue through separately authorized grandMA2 and Yamaha DME7 identification. Tests assert that IP addresses, MAC data, and interface identity remain absent from stored path-free results and emitted events while the exact responder remains available only in Agent SQLite.

Protocol 1.6 adds a second manager action for one approved proposal. The Agent resolves its CIDR locally and performs reachability-only ICMP checks against at most 32 usable addresses, with 100-500 ms per-host timeouts and concurrency capped at eight. Responding addresses are retained only in Agent SQLite and keyed by the exact authorization command and proposal. Durable path-free results and control-plane events contain only attempted and responding counts; they contain no host addresses, ports, banners, or product claims, and discovery performs no synchronization. Rejecting the proposal removes its locally retained host sets.

Protocol 1.7 adds a separate manager action for grandMA3 identification after a completed bounded discovery with at least one responder. The command references the opaque proposal and exact discovery command only. The Agent reads that local responder set, checks the officially documented grandMA3 Web Remote HTTP service on port 8080 with 100-500 ms timeouts, and requires a `grandMA3` response signature. Addresses and matches remain in Agent SQLite. The path-free completion contains only attempted and identified counts plus the `grandMA3` product family. The probe does not authenticate, enumerate a session, synchronize state, retain response content, or claim grandMA2 support.

The control plane correlates identification outcomes to the exact Agent, proposal, discovery authorization, and pending identification command. It persists bounded path-free pending/completed/failed state, attempted and matched counts, product-family evidence, failure details, and completion time. Native onboarding polls pending work and displays the result for operator review without receiving host addresses. A new proposal decision or bounded discovery clears stale identification evidence.

Protocol 1.8 adds a separate manager-authorized Yamaha DME7 check using only the responders retained by one exact bounded discovery. The Agent uses Yamaha's documented TCP port 49280 and sends only the documented LF-terminated `devinfo productname` and `devinfo manufacturer` read queries. A match requires exact `DME7` and `Yamaha Corporation` responses. Addresses, matches, and bounded response handling remain Agent-local; path-free completion contains attempted/matched counts and `Yamaha DME7`. An open port, partial response, another Yamaha model, or generic reachability is not product evidence.

Yamaha results use independent tenant-scoped pending/completed/failed state and exact Agent/proposal/discovery/identification correlation. Native onboarding offers a separate Yamaha action, polls pending work, and displays bounded path-free counts and family evidence without overwriting grandMA3 results. A new subnet decision or reachability discovery clears both product results because their responder authorization is stale.

Protocol 1.9 adds independently authorized grandMA2 identification against the same exact Agent-local responder boundary. MA Lighting's official grandMA2 Telnet Remote documentation assigns TCP 30000 to the command line, requires login before commands, and shows the unauthenticated server greeting. The Agent connects, sends zero bytes, reads at most 4,096 banner bytes with a 100-500 ms timeout, and requires the documented guest/login-prompt combination before returning `grandMA2`. Telnet Remote must already be enabled, so a disabled service is a safe false negative. An open port, generic Telnet banner, partial greeting, grandMA3 behavior, or reachability is not evidence.

grandMA2 results have independent exact command correlation and pending/completed/failed review state. Host addresses and response bytes remain Agent-local; the control plane receives only attempted/matched counts and bounded `grandMA2` evidence. A new decision or discovery clears grandMA2, grandMA3, and Yamaha results as stale.

Automated synthetic console/onPC fixtures now validate the complete documented greeting, a disabled listener, a silent service at the 100 ms lower timeout bound, fragmented responses, partial guest-only and login-only greetings, a generic Telnet banner, a grandMA3 lookalike, and a valid signature placed beyond the 4,096-byte cap. Every connected fixture asserts that the Agent sends zero bytes. These fixtures do not replace validation against representative real console/onPC versions, which remains deferred until explicitly authorized.

L-Acoustics network identification is deliberately deferred after official-evidence review. Public L-Acoustics material confirms that LA Network Manager and LA Device Scanner discover and identify units, but does not publish their wire contract or a demonstrably read-only query; the same tools also configure, synchronize, rename, or change network settings. The documented Electronics HTTP API could become a defensible product-specific boundary, but its contract is available only after identity submission and separate terms acceptance, and the public page does not establish a safe identity endpoint or response signature. ShowVault sends no L-Acoustics probe and does not treat an open port, generic HTTP, Milan/AVDECC or Dante metadata, or reachability as L-Acoustics evidence. Reconsider only with terms-authorized primary API documentation and a representative fixture, or a newly published read-only protocol contract.

Protocol 1.13 adds reusable PJLink projector identification after a manager separately authorizes the operation for responders from an approved bounded discovery. The Agent connects to standard PJLink TCP port 4352 with a 100–500 ms per-host timeout and at most 32 already-authorized hosts. It proceeds only after the unauthenticated `PJLINK 0` greeting, sends the standardized read-only `%1INF1 ?` manufacturer query followed by `%1INF2 ?` model query, and caps every response line at 256 bytes. JBMIA's official [PJLink specification](https://pjlink.jbmia.or.jp/english/data/5-1_PJLink_eng_20131210.pdf) defines those queries. Christie's official [LX41/LW41 technical guide](https://www.christiedigital.com/globalassets/resources/public/020-000507-01-christie-lx41_lw41-user-manual-technical-guide-multi-language.pdf) documents exact manufacturer response `CHRISTIE` and model responses `LX41` or `LW41`; only those pairs produce `Christie LX41` or `Christie LW41`. Authentication-enabled greetings, partial/malformed responses, other manufacturers, and unlisted Christie models are safe false negatives. Addresses and raw responses remain in Agent-local SQLite; emitted results contain only bounded counts and exact product families. The endpoint does not power, configure, validate, back up, verify, or restore a projector, and the first slice does not yet persist projector-specific completion state on the subnet proposal or expose a dashboard action.

Barco projector identification is deliberately deferred after primary-source review. The official [G60 network-control guide](https://www.barco.com/manuals/R5910888/f0kx8dfvpl17.html) confirms PJLink v1.0 on TCP port 4352 but directs readers to Barco Support for supported-command details. The official [G60 installation-manual record](https://www.barco.com/en/support/docs/r5910887) identifies the covered G60 family, but the available command list names standard `INF1` and `INF2` queries without publishing their literal response strings. Barco's official [Pulse PJLink guide](https://www.barco.com/manuals/R5906795/u5nxou2pc9od.html) confirms PJLink support while stating that disabling authentication is not advised. ShowVault therefore does not guess whether manufacturer identity is returned as `BARCO`, `Barco`, `Barco NV`, or another value, does not infer model strings from marketing names, and does not weaken authentication. No Barco signature is added to the PJLink allowlist, no real projector is contacted, and no identification, configuration, validation, backup, verification, or restore support is credited. Reconsider when an official response table or authorized representative fixture establishes exact `INF1`/`INF2` values.

Panasonic projector identification extends the same protocol 1.13 boundary. Panasonic's official [PT-DZ770 operating instructions](https://www.panasonic.com/content/dam/Panasonic/support_manual/Projectors/English/PT-DZ770ES_User_Manual_pt-dz770esoperatinginstructions_1959238531_.pdf) document exact PJLink responses `Panasonic` and `DZ770`; the official [PT-VW431DEA operating instructions](https://www.panasonic.com/content/dam/Panasonic/support_manual/Projectors/English/PT-VW431DEA_User_Manual_pt-vw431deaoperatinginstructions-compl_1160540057_.pdf) document `Panasonic` and `VW431DEA`; and the official [PT-RZ470/PT-RW430 operating instructions](https://www.panasonic.com/content/dam/Panasonic/support_manual/Projectors/English/PT-RZ470_Operating_Instructions.pdf) document `Panasonic` paired with `RZ470` or `RW430`. Only those exact, case-sensitive pairs produce a Panasonic product family. The Agent uses only the existing unauthenticated, read-only `INF1`/`INF2` flow against already-authorized responders; no real hardware was contacted. Authentication-enabled greetings, other models, guessed casing, partial responses, and generic PJLink support remain safe false negatives. Addresses and raw responses remain Agent-local, and no configuration, validation, backup, verification, or restore support is credited.

Epson projector identification extends that same bounded protocol 1.13 allowlist. Epson's official [QB1000 PJLink documentation](https://files.support.epson.com/docid/cpd6/cpd64535/source/network/concepts/pjlink.html) publishes manufacturer response `EPSON` and model responses `EPSON QB1000B` or `EPSON QB1000W`. Only those exact, case-sensitive pairs produce `Epson QB1000B` or `Epson QB1000W`; no other Epson model is inferred. The existing unauthenticated, read-only `INF1`/`INF2` flow, manager-authorized responder set, host and timeout caps, Agent-local addresses/raw responses, and path-free results are unchanged. Synthetic fixtures cover accepted pairs, unknown models, incorrect casing, and unrelated manufacturers; no real projector was contacted, and no configuration, validation, backup, verification, or restore support is credited.

Digital Projection network identification is deliberately deferred after primary-source review. The official [E-Vision 10000i WU product page](https://www.digitalprojection.com/en/projectors/e-vision-10000i-wu/) links an [E-Vision 8000i/10000i control-protocol workbook](https://digitalprojection.co.uk/dpdownloads/User%20Guides/E-Vision-10000i/124-034A%20Protocol%20Commands%20E-Vision%208000i%2010000i.xlsx). That workbook documents a read-only `model.name ?` command on TCP port 7000 but constrains the response only as `<string>`, without a literal manufacturer or model value. Its separate UDP discovery contract broadcasts responses and includes IP address, hostname, MAC address, ID, and serial data; the one example names `HIGHLite 660`, not either E-Vision model covered by the workbook. ShowVault therefore does not guess `model.name` values, reinterpret an unrelated example as an exact signature, add a broadcast or separate Digital Projection scanner, or publish privacy-bearing response fields. No real projector was contacted and no identification, configuration, validation, backup, verification, or restore support is credited. Reconsider when an official literal response table or authorized representative fixture establishes an exact read-only identity boundary that can remain limited to already-authorized hosts.

NEC projector identification extends the same manager-authorized protocol 1.13 operation without adding a discovery path. Sharp NEC's official [Projector Control Command Reference Manual](https://business.sharpusa.com/Portals/0/downloads/Manuals/sharp-pj-control-command-codes.pdf) defines TCP port 7142, the fixed read-only Base Model Type request `00 BF 00 00 01 00 C0`, the fixed-length response envelope, and its low-byte checksum. The official [NEC command appendix](https://www.sharpnecdisplays.eu/p/download/cp/Products/Projectors/Shared/CommandLists/NEC-ExternalControlManual-english.pdf?fn=NEC_ExternalControlManual-english.pdf) maps exact response bytes `FF 30 00 10`, `FF 30 01 10`, `FF 35 00 10`, and `FF 35 00 11` to NP-PH3501QL, NP-PH2601QL, NP-PX2000UL, and NP-PX2201UL respectively. Only those four exact checksummed signatures produce a family. The NEC and PJLink probes run concurrently within the existing 100–500 ms per-host timeout against the same maximum 32 already-authorized responders; an open port, malformed or partial response, invalid checksum, unlisted model type, and generic PJLink support remain safe false negatives. Addresses and raw response bytes remain Agent-local, emitted results remain path-free, no real projector was contacted, and no configuration, validation, backup, verification, or restore support is credited.

Sony projector model identification is deliberately deferred after primary-source review. Sony's official [BPJ Protocol Manual Common](https://pro.sony/s3/2018/07/03140912/Sony_Protocol-Manual_1st-Edition-Revised-2.pdf) documents the read-only PJLink identity queries, fixes the normal `INF1` manufacturer response as `SONY`, but describes `INF2` only as an unspecified model name of at most 32 characters. That is insufficient for ShowVault's exact manufacturer/model allowlist: accepting every model string paired with `SONY`, or guessing literals from marketing names, would weaken the existing evidence boundary. The same manual sets PJLink authentication to enabled by default; ShowVault continues to treat the authenticated `PJLINK 1` greeting as a safe false negative and does not use the published default password or ask an operator to disable authentication. Sony's SDAP service periodically broadcasts product name, serial number, location, community, and power status, so it is not substituted for the existing target-bounded probe and its privacy-bearing fields are not collected or published. No scanner, fixture, implementation, real-device contact, or identification, configuration, validation, backup, verification, or restore support is added. Reconsider when an official model-specific protocol table or authorized representative fixture establishes exact `INF2` literals within the existing bounded read-only flow.

Protocol 1.14 adds separately authorized Blackmagic Smart Videohub 16x16 identification without adding a discovery path. Blackmagic Design's official [Videohub Ethernet Protocol](https://documents.blackmagicdesign.com/DeveloperManuals/VideohubEthernetProtocol.pdf?_v=1698822010000) specifies TCP port 9990 and states that a Videohub Server sends a complete status dump immediately upon connection. The Agent therefore sends zero bytes and reads at most 4,096 response bytes with the existing 100-500 ms per-host timeout from at most 32 responders retained by the exact authorized discovery command. A match requires the documented first `PROTOCOL PREAMBLE` block with version `2.3`, followed by a complete `VIDEOHUB DEVICE` block containing `Device present: true`, exact model `Blackmagic Smart Videohub`, 16 video inputs, zero processing units, 16 video outputs, zero monitoring outputs, and zero serial ports. Only that complete signature produces `Blackmagic Smart Videohub 16x16`; conflicting duplicate fields, partial blocks, other protocol versions, other models or capacities, silent services, and an open port are safe false negatives. Addresses stay in Agent-local SQLite and raw status bytes are not published. The control plane persists independent tenant-scoped pending/completed/failed state and receives only attempted/matched counts plus the bounded family, and native onboarding exposes a separate manager action. The pre-existing `showvault.blackmagic-atem` local plugin remains an operator-configured ATEM XML-state recovery path and is not network identity evidence. No real hardware was contacted, and no routing control, configuration, validation, backup, verification, or restore support is credited by this identification slice.

Sony broadcast-device network identification is deliberately deferred after primary-source review. Sony's official [LMD-1951MD command-protocol manual](https://pro.sony/support/res/manuals/4291/c9cdab7e8d7096b88089f19edb3e0f42/42918481M.pdf) documents SDCP over TCP port 53484 as a peer-to-peer request/response transport, but its published VMC status commands expose operating state rather than an exact model-identity request with a literal LMD-1951MD response. Treating a valid SDCP response, its monitor category, default community, or an open port as Sony product identity would therefore be generic reachability rather than exact evidence. The same manual defines SDAP as a periodic UDP broadcast carrying product name together with serial number, location, community, power, connection IP, acceptable IP ranges, and other device fields; ShowVault does not add that broadcast listener or collect those privacy-bearing fields. Sony's official [XVS-9000 product material](https://pro.sony/ue_US/products/video-switchers/xvs-9000) documents AMWA NMOS support and optional SNMP Agent Software but does not publish an exact read-only product-identity request/response that can be confined to the already-authorized responder set. Projector and PTZ/camera protocols remain separate catalog scopes and are not used as broadcast-device evidence. No scanner, fixture, implementation, real-device contact, or identification, configuration, validation, backup, verification, or restore support is added. Reconsider when an official broadcast-product protocol table or authorized representative fixture establishes an exact read-only identity signature within a bounded manager-authorized flow.

Protocol 1.15 adds separately authorized NewTek TriCaster TC1 identification without adding NDI, multicast, broadcast, or general HTTP discovery. The official [Automation, Integration & Control Guide](https://downloads.newtek.com/LiveProductionSystems/VMC1/Automation%20and%20Integration%20Guide.pdf) documents a read-only `GET /version` request and an exact example response whose direct `product_information` children include `product_model` value `TC1` and `product_name` value `TriCaster TC1`. The Agent sends that request on TCP port 80 only to the at most 32 responders retained by one exact completed manager-authorized discovery, uses the existing 100-500 ms per-host timeout, rejects redirects and non-200 responses, caps the body at 16,384 bytes, prohibits DTD processing, and requires exactly one matching model and name element. The response also contains product ID, serial, machine, software/build, and session data; addresses and the full XML remain in Agent-local SQLite while the control plane receives only attempted/matched counts and `NewTek TriCaster TC1`. The guide says web password protection is enabled by default. ShowVault does not send the documented default credentials, ask operators to disable protection, or persist credentials; an authentication challenge is a safe false negative. Other TriCaster models, malformed/oversized responses, duplicate or conflicting fields, generic NDI metadata, ports, HTTP titles, configuration/control, backup, verification, and restore remain unsupported. The control plane persists independent tenant-scoped pending/completed/failed state and native onboarding exposes a separate manager action. Synthetic fixtures only were used; no real NewTek hardware was contacted.

AJA Video Systems broadcast-device network identification is deliberately deferred after primary-source review. AJA's official [REST API documentation](https://gitlab.aja.com/pub/rest_api) publishes a target-bounded HTTP GET interface at `/config?action=get&paramid=...`, but the generic guide says parameters and their descriptors vary by product and firmware, and it does not publish a model-identity parameter with literal response values for any covered HELO or Ki Pro model. A response from `/config`, `/desc.json`, `/descriptors.html`, or the hidden developer page would therefore prove only implementation of AJA's generic control framework, not an exact product identity. AJA's official [IPR-10G-HDMI discovery guidance](https://www.aja.com/support/item/6210) instead lists NMOS discovery/control and AJA REST discovery through SSDP or mDNS; ShowVault does not add multicast discovery, accept user-configurable device names or generic NMOS/REST participation as identity, or collect network-wide advertisements. The HELO-specific REST material also documents password authentication and session cookies; ShowVault does not submit credentials, weaken authentication, or retain sessions for identification. No scanner, protocol fixture, runtime/schema change, real-device contact, or identification, configuration, backup, verification, or restore support is added. Reconsider when AJA publishes a read-only model query with an exact literal response, or when an explicitly authorized representative fixture and vendor contract establish the same bounded signature.

## Next slices

1. Research official primary sources for dependable automatic local application and recoverable-project/media roots for the TinkerList row under Show control and playback. Preserve the existing catalog-driven, path-free control-plane boundary; do not infer installation or recovery support from process names, generic files, user-entered paths, or undocumented platform conventions.
